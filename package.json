{
  "name": "ace-coding-practice-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @ace/backend",
    "dev:backend": "npm run dev --workspace @ace/backend",
    "dev:frontend": "npm run dev --workspace @ace/frontend",
    "build": "npm run build --workspace @ace/shared && npm run build --workspace @ace/backend && npm run build --workspace @ace/frontend",
    "lint": "npm run lint --workspace @ace/shared && npm run lint --workspace @ace/backend && npm run lint --workspace @ace/frontend",
    "typecheck": "npm run typecheck --workspace @ace/shared && npm run typecheck --workspace @ace/backend && npm run typecheck --workspace @ace/frontend"
  },
  "engines": {
    "node": ">=20"
  }
}

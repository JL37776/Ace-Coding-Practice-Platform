name: Deploy to server

on:
  push:
    branches: [main, master]
  workflow_dispatch:

concurrency:
  group: ace-server-deploy
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install
        run: npm ci

      - name: Build
        run: npm run build

      - name: Install SSH password client
        run: |
          sudo apt-get update
          sudo apt-get install -y sshpass

      - name: Configure SSH
        env:
          PHONE_SSH_HOST: ${{ vars.PHONE_SSH_HOST }}
          PHONE_SSH_PORT: ${{ vars.PHONE_SSH_PORT }}
        run: |
          set -euo pipefail
          test -n "$PHONE_SSH_HOST"
          port="${PHONE_SSH_PORT:-10022}"
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          echo "Scanning SSH host key for ${PHONE_SSH_HOST}:${port}"
          if ! ssh-keyscan -T 20 -p "$port" "$PHONE_SSH_HOST" >> ~/.ssh/known_hosts; then
            echo "::warning::ssh-keyscan failed for ${PHONE_SSH_HOST}:${port}. The deploy step will retry with StrictHostKeyChecking=accept-new."
          fi

      - name: Package release
        run: |
          tar \
            --exclude='.git' \
            --exclude='node_modules' \
            --exclude='**/node_modules' \
            --exclude='*.log' \
            -czf /tmp/ace-coding-release.tgz .

      - name: Upload release
        env:
          PHONE_SSH_HOST: ${{ vars.PHONE_SSH_HOST }}
          PHONE_SSH_PORT: ${{ vars.PHONE_SSH_PORT }}
          PHONE_SSH_USER: ${{ vars.PHONE_SSH_USER }}
          PHONE_SSH_PASSWORD: ${{ secrets.PHONE_SSH_PASSWORD }}
        run: |
          set -euo pipefail
          test -n "$PHONE_SSH_HOST" || { echo "::error::Repository variable PHONE_SSH_HOST is empty"; exit 1; }
          test -n "$PHONE_SSH_PASSWORD" || { echo "::error::Repository secret PHONE_SSH_PASSWORD is empty"; exit 1; }
          sshpass -p "$PHONE_SSH_PASSWORD" scp -P "${PHONE_SSH_PORT:-10022}" \
            -o StrictHostKeyChecking=accept-new \
            -o ConnectTimeout=20 \
            /tmp/ace-coding-release.tgz \
            "${PHONE_SSH_USER:-Ubuntu}@${PHONE_SSH_HOST}:/tmp/ace-coding-release.tgz"

      - name: Deploy and restart
        env:
          PHONE_SSH_HOST: ${{ vars.PHONE_SSH_HOST }}
          PHONE_SSH_PORT: ${{ vars.PHONE_SSH_PORT }}
          PHONE_SSH_USER: ${{ vars.PHONE_SSH_USER }}
          PHONE_SSH_PASSWORD: ${{ secrets.PHONE_SSH_PASSWORD }}
          PHONE_DEPLOY_PATH: ${{ vars.PHONE_DEPLOY_PATH }}
        run: |
          set -euo pipefail
          test -n "$PHONE_SSH_HOST" || { echo "::error::Repository variable PHONE_SSH_HOST is empty"; exit 1; }
          test -n "$PHONE_SSH_PASSWORD" || { echo "::error::Repository secret PHONE_SSH_PASSWORD is empty"; exit 1; }
          sshpass -p "$PHONE_SSH_PASSWORD" ssh -p "${PHONE_SSH_PORT:-10022}" \
            -o StrictHostKeyChecking=accept-new \
            -o ConnectTimeout=20 \
            "${PHONE_SSH_USER:-Ubuntu}@${PHONE_SSH_HOST}" \
            "PHONE_DEPLOY_PATH='${PHONE_DEPLOY_PATH:-/opt/ace-coding/current}' bash -s" <<'REMOTE'
          set -euo pipefail
          app_dir="${PHONE_DEPLOY_PATH:-/opt/ace-coding/current}"
          sudo mkdir -p "$app_dir"
          sudo chown -R "$(id -un):$(id -gn)" "$(dirname "$app_dir")"

          if [ -f "$app_dir/apps/backend/.env" ]; then
            cp "$app_dir/apps/backend/.env" /tmp/ace-backend.env
          fi
          find "$app_dir" -mindepth 1 -maxdepth 1 -exec rm -rf {} +
          tar -xzf /tmp/ace-coding-release.tgz -C "$app_dir"
          if [ -f /tmp/ace-backend.env ]; then
            mv /tmp/ace-backend.env "$app_dir/apps/backend/.env"
          fi

          cd "$app_dir"
          export npm_config_cache=/tmp/ace-coding-npm-cache
          npm ci --omit=dev
          chmod +x infra/deploy/phone/*.sh
          infra/deploy/phone/start-ace-coding.sh
          infra/deploy/phone/start-aws-9999-tunnel.sh
          curl -fsS http://127.0.0.1:3100/api/health >/dev/null
          curl -fsS http://127.0.0.1:8080/api/health >/dev/null
          REMOTE
